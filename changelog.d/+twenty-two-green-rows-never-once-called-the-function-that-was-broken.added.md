`still.py verify` held twenty-two rows while the field standing for the other
parser read `0 oracle(s)` on every board ever taken, and there is nothing wrong
with any of the twenty-two. **Every one of them hands `differ` a `Witness`
constructed by hand.** They prove the predicate and are structurally incapable of
saying anything about the filler, because they never call it — and the filler is
the half that ships.

Seventeen new rows drive `still.take` itself over a real grammar tree: a real
`attest.consult`, a real record, each field read back after one thing was caused.
An authored byte flipped, so the identity moves. The generated parser replaced
under an identity that holds, so `lowered` moves and `oracles` does not. The
identity rule swapped, so `rule` moves and the pair refuses as incomparable
rulers rather than as thirty drifts. The workspace and the seat set in the
environment. One artifact fed, one artifact witnessed — and, adversely, the
witness feeding nothing of its own into the ledger it is about to read, because a
record that changes what it records is the defect this whole file is about.

The first two rows are the hole restored. One fed ledger, one process, read by
both population rules at once: the retired rule says `0 oracle(s)` and the
current one says `1`. Keeping the retired rule beside the new one is the same
idiom `attest.was` and `stamp.py --verdicts` use, and the reason is the same — a
row that shows only the new rule working proves the new rule is self-consistent,
which every rule is.

Checked by breaking it: with the fallback removed and the shipped bug restored,
exactly two rows go red and all twenty-two of the originals stay green.

Hermetic, in a scratch tree. `attest.survey` derives a grammar's root from the
files themselves, so a copy spells its files exactly as the live tree does — and
an instrument that had to write into `lang/` to test itself would be the seventh
instance of the shape it exists to catch.
