"Fourteen rows, no collateral" was cleared on thirty-one columns of `damage`,
`built` and `standing` - every one of them outliner's own words about its own
forest - because all twenty arms of the family read `square 0`. Re-taken with
`standing.py --audit` paid inside each arm's own work dir, **29 of 30 rows are
sighted in all twenty-one arms and the clearance holds**: every arm moves
exactly the grammars its rows seat and **no other grammar on any of twenty-four
columns**, 13,728 cells, zero differences.

A second check, which consults no oracle at all, agrees byte-for-byte: every
arm's thirty parse trees were captured and diffed against base. Each single and
pair arm is **tree-identical on 29 of 30 grammars**, and the union arm - all
fourteen rows out at once - on **21 of 30**, exactly the twenty-one nobody
seated. That covers `yaml`, which no board can grade, and every unaudited byte
on the other twenty-nine.

The union also **decomposes exactly**: union scala equals the scala pair arm to
the byte, union julia equals the julia pair, union kotlin the kotlin pair, and
so on for all nine owners. Fourteen rows removed at once do precisely what they
do one at a time.

What did not survive is the pricing. haskell's row is worth 9,168 on `damage`
and **5** on `square`; elixir's `_newline_before_do` is worth 1,329 on `damage`
and **23,878 - the whole of elixir's agreement with tree-sitter**; ocaml's
comment row **changes sign**, costing 721 damage while gaining 448 square. Five
of fourteen rows are worth more than twice as much sighted as blind, and all
five seat an extra or a separator, which is the direction the mechanism predicts:
a correctly-recognised comment is an orphan and a misread one is `built`.

`research/joinery/consort/RESULT-8-sighted.md`, driven by `sighted.py` and
falsified by `collateral.py`. Predictions in `PREDICTION-3-sighted.md`, scored
four right, four half, one wrong, one never tested.
