The wrong-limb defect handed to this lane says gather takes the declaration limb
at state 1762 on `=`. Gather does not choose it. That cell is `residual`, and
`Forks.of` admits `declared` and `unwritten` cells only - so **the other limb is
never offered**, and `x = 0;` inside an `always` block seats as a
`block_item_declaration` with no fork anywhere in the parse. Four of the seven
wrong statements in the new witness table are `=` rows and none of them is
reachable from quire.

The `<=` row is reachable, and there the defect is exactly what was described:
the cell is a **declared** reduce_reduce, gather forks, and one trace line later
`collapse` merges the two readings and keeps the limb the press picked at a cell
the author declared arbitrary. `x <= 0;` ships as a `clocking_drive`.

Both directions out of that merge are now closed by measurement rather than by
argument. Keeping the *higher* rank seats two more verilog rows and costs
**+677 bytes** of corpus damage and **23,985 bytes** of agreement with
tree-sitter - elixir falls from 23,879 square bytes to one, and php stops tying
upstream. Declining the merge for differing-rank twins costs **+57,627 bytes**
over five grammars, because the extra readings fill `crowd` and the later forks
that mattered are denied, and it hands back **the same trees** - so `collapse`
is load-bearing, `twinned`'s soundness argument stands, and it is the tie-break
rather than the merge that picks a limb.

What lands: `.local/limb/limb.py`, which reports what node a statement *became*
rather than whether its module stood - five of verilog's seven wrong rows parse
whole, which is how they were being scored as seated - the two boards, and
`collapse`'s docstring carrying both results so nobody re-derives them.
`research/joinery/verilog/RESULT-5-merge.md`, including the stale-baseline
mistake that first read arm C as a win: on a tree ten lanes write to, the
control has to be pinned *after* the arm, or the window belongs to whoever else
landed in it.
