A refused token now reports the folds it drove to reach its wall, so `inquest`
can name the cell that killed a parse instead of saying it cannot rule the press
out. `walk` kept that chain and is test-only; `quire`, the loop the CLI runs,
walked the same states and discarded them, and every verdict over a table with
any damage on that terminal degraded to a `press?` naming a suspect.

Four of the five walls that printed that sentence were not press defects. With
the chain in hand zig proves out as the one genuine frayed `open` cell, verilog
and swift join sql as `weave` - empty under every split - and julia is an
`oracle` case whose fork the table still offers. Every row of the board is
byte-identical: this records what the loop already walked.

`folded` is optional rather than a plain slice, because empty already means
something - verilog's backtick drove no reduces at all, which is a fact about
that wall, where null is nobody having looked.
