`Scanner.declined` is our own engine refusing a pattern, and its doc comment
sized the population from a php reading that has since closed - "eighty-seven",
"far more common than anyone assumed". Measured across all thirty grammars, the
corpus now declines **one** pattern: markdown's `entity_reference`. php declines
none.

Both comments now say what is true, and say why the field stays separate at a
count of one: splitting `declined` from `blind` is what let the population be
counted, and counting it is what closed it. One is a number somebody can finish.

`blind` - externals, which are someone else's C - is 167 over fifteen grammars
and is unchanged.
