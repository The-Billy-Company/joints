`research/joinery/haskell/RESULT-2-cost.md` prices `c589a31` (*haskell's
parser-issued brackets*) on the axis its own dossier said nobody had asked.
`RESULT-1-brackets.md` closed by naming the gap - *nothing here checks the new
nodes against the oracle* - and this is that check, taken as an isolation arm at
`c589a31^` in a detached worktree, judged by today's `tool/` and the same court
`d85e736fa`, 30 of 30 verdicts live on both arms.

Thirty rows, twenty-nine byte-identical. haskell alone moves, and the +6,008
`built` bytes decompose exactly:

| bucket | before | after | delta |
|---|---|---|---|
| square | 5 | 1 | **−4** |
| crooked | 2,029 | 5,109 | **+3,080** |
| soft | 86 | 1,590 | +1,504 |
| unframed | 6,799 | 8,226 | +1,427 |
| unaudited | 0 | 1 | +1 |

`3,080 + 1,504 + 1,427 + 1 − 4 = 6,008`, and **`square` is the only bucket that
received none of it.** `damage` fell by the same 6,008, so the seat really is
converting unbuilt bytes into structure - the commit's `nodes 6868 → 12051`
claim survives intact - but the oracle defends none of the structure it built
and calls 3,080 bytes of it structurally wrong.

Where it goes the wrong way, in the same paragraph as the win: on coverage this
is unambiguous (`standing` 26.05% → 43.60%, `roots` −25%), and the corpus's
entire `crooked` delta for the day is this one row, 30,573 → 33,653. That also
resolves a discrepancy - a board reading 33,653 against the elixir dossier's
30,573 is not two lanes disagreeing, it is one commit landing between them. The
before-arm reproduces that dossier's `built 401,115 / crooked 30,573` to the
byte on a different binary in a different worktree, which is the corroboration
the attribution rests on.

haskell's `trued` reads 0.0% on both arms. The seat did not lose the agreement
axis; haskell has never had it, and that is the open question the page hands on.
