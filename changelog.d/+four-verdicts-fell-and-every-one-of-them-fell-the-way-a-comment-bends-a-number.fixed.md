The ranked worklist in `consort/RESULT-9-reach.md` is worked. Ten pages settled
by hand against the sighted arms already on disk, nothing re-measured, and
`crooked` quoted nowhere as load-bearing.

**Four verdicts fell, and all four fell in one direction** — the direction the
mechanism predicts, since a correctly-recognised extra leaves the tree as an
`orphan` while a misread one is counted as `built`:

- **`bench.report.md`** called elixir the rubble table's finished row: 0 rubble,
  100% standing, 0 damage. Elixir is **51.8% `trued`** with 22,210 bytes built and
  uncorroborated.
- **`consort/RESULT-3-scala.md`** called scala's pair *two overlapping
  regressions*. Both rows are positive sighted — **+6,739 and +6,536 `square`**,
  each worth nearly all the agreement scala has. Two sign flips in one page, the
  largest inversion on the tree, on a file that is 79% block comment.
- **`semi/RESULT-2-seated.md`**'s kotlin *"smaller regression inside a larger
  win"* is withdrawn: kotlin finishes at **98.6% `trued`**, 247 bytes
  uncorroborated in total. The −1,075 `built` was the comment bias, and the page's
  refusal to launder it as a metric artifact was right discipline reaching the
  wrong verdict.
- **the ocaml fragment** carries its withdrawal: −721 `damage` is **+448
  `square`**.

Two more are **re-pointed, not overturned**. `verilog/RESULT-1-wall.md` keeps its
headline and gains the number that governs everything downstream: of the 32,193
bytes verilog builds, **2,184 are `square`, 7.9% of what the oracle adjudicates**
— it could not have been written sighted, because verilog was 100% unadjudicable
until this afternoon. `layout/RESULT-1-scala.md`'s *40.8% → 79.4% standing* is
reach, not assent; scala finishes at **33.5% `trued`**.

**Five pages under-priced themselves, every one by seating an extra or a
separator.** php's single mend in `orphan/RESULT-1-gate.md`, called *"twelve
percent of its structural account"* at 8,091 orphan bytes, is worth **67,183
`square`** — the whole file's agreement, **8.3×**. elixir's caesura row is 18×.
And `orphan/RESULT-2-wall.md`'s explicit bet — *"the number I would bet on and not
the number I would report as measured, and the only way to promote it is to build
the thing"* — is promoted: the thing was built and it is **+27,143 `square`**
against the 20,728 `built` estimated, low by 31%.

**Six pages hold, explicitly**, and the one that holds best is why the rest are
suspect: `orphan/RESULT-2-wall.md`'s subject is kotlin at 98.6% `trued`, so the
mechanism it established — a correct extra is an `orphan`, a misread one is
`built` — is now the rule the four casualties above were judged by.
`consort/RESULT-10-record.md`.
