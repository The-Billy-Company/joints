Every collateral claim made today by a change that **cannot move a pressed table**
is now supported by an instrument that could have said otherwise. Fourteen
`Troupe` rows were seated across nine grammars; each was re-measured against its
own isolation arm - today's tree with exactly that row deleted and every seam,
walk, dialect and vein left standing - built by `pin.py` into its own prefix and
read by `standing.py` with its own `OUTLINER_WORK` and its own `OUTLINER_LANE`.

**Thirteen arms move exactly one grammar, one moves none, and in every case the
grammar that moved is the grammar the row was seated for.** No arm moved a second
grammar by a single column of thirty-one. The union arm - all fourteen rows out at
once - moves nine of thirty grammars and leaves the other twenty-one
byte-identical, and those nine are exactly the nine carrying a row seated today,
with no tenth.

| seating | grammar | worth, in bytes of damage |
|---|---|---|
| `_quoted_content_double/.marrow/.elixir_quoted` | elixir | −21,201 |
| `_string_start/.fence/.kotlin` | kotlin | −20,728 |
| `_automatic_semicolon/.caesura/.kotlin` | kotlin | −19,204 |
| `_implicit_semi/.caesura/.swift` | swift | −10,687 |
| `_cmd_layout_start/.writ` | haskell | −9,192 |
| `_immediate_paren/.abut` | julia | −9,094 |
| `encapsed_string_chars/.marrow/.php_encapsed` | php | −8,699 |
| `_indent/.offside/.slashes` | scala | −7,763 |
| `_content_str_1/.marrow/.julia_quoted` | julia | −5,471 |
| `block_comment/.marrow/.kotlin_block` | scala | −2,407 |
| `_newline_before_do/.caesura/.elixir` | elixir | −1,329 |
| `_trivia_raw_env_verbatim/.marrow/.latex_verbatim` | latex | −1,185 |
| `comment/.marrow/.ocaml_comment` | ocaml | **+721** |
| `multiline_comment/.marrow/.swift_block` | swift | 0 |

The two ways this could have gone wrong both came out clean. `kotlin_block`'s vein
is guest-listed `{kotlin, scala}` and does reach scala - by 2,407 bytes, in the
direction of fixing it, and nothing else. And ocaml is the only grammar on the
board whose damage rose today, 1,461 → 2,182 with standing 91.34% → 87.07%; that
cost was already argued in `bench.report` and carried in `orphan/RESULT-2-wall.md`,
so the day's one regression was published rather than hidden by a check that could
not see it. It is also the one seating with no fragment of its own, which is the
wrong row to leave out.

Kotlin's two rows are not additive: removing *either* costs about 20,000 bytes.
A seating is a permission the walk needs, not a contribution to be summed, which
is why fourteen arms were needed where the union arm can only count nine grammars.
So the `worth` column above may not be added down. Every figure in it is a
marginal - what the row buys given the other thirteen are seated - and kotlin's
two price the same 19,678-byte stretch of `Maps.kt` twice, because the file walls
at byte 245 without the caesura and at byte 270 without the fence with all 35,571
remaining bytes behind both. Kotlin's whole defect is 20 KB and these two rows
claim 40. Split so it sums, the fence is worth +10,593 and the caesura +9,085
(`research/joinery/consort/RESULT-1-kotlin.md`).

The recited inventory was short by two - `comment/.marrow/.ocaml_comment` and
`block_comment/.marrow/.kotlin_block` are in the roster and in `bench.report` and
in no fragment - and wrong about haskell, whose `writ` layout did land today and is
the fifth-largest seating on the board.
