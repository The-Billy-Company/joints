A ring is a promise that a later parse can stand up where this one stood, and
`holds` verifies it by re-lexing the recorded stretch. It asked with the state
row - every lookahead the table holds - where the original parse asked with
`offer`'s admissions, which walked the stack and kept only what could shift.
The wider question gets different answers: a wide probe reads `=` where the
parse read `=>`, and wakes hands the parse never woke. So unchanged bytes
"re-lexed differently", every typescript ring declined, and the incremental
path on that grammar was 153.8x tree-sitter - an edit costing 88% of reopening
the file. Not slow; barely incremental.

Every token now records the veil it was lexed under - the interned `named`
mask of the exact slate `offer` built - and `holds` replays that, clear plus
the veil's admissions, which rebuilds the whole `Expected` deterministically.
A supply's ghost records the veil of the token whose refusal asked for it,
so the array stays parallel and a ring over a supply declines exactly as
before.

All nine measurable grammars now beat tree-sitter on the keystroke axis,
0.17x to 0.85x - typescript at 104 us against their 408.4, from 66,224.
