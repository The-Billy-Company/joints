latex declared twelve externals and seated none of them, so every raw region was
blind: `ltnews01.tex` parsed to 72 roots, 77.4% standing, 108 of 4,061 built
bytes square.

All twelve are `find_verbatim(lexer, KEYWORD, is_command_name)` — one walk,
twelve rows of data, `serialize` returning 0. That is `marrow.Family`, php's
animal, with one obstacle: **it closes on a string where `Mark.shut` is a
`u8`**. Four places read that byte (`matter`, `quoted`, `encapsed`, and
`outside.shutOf`, which keys a family's closing terminal *by* it), so `shut`
stayed the close's first byte and gained a `tail` for the rest. The three
existing walks never read `tail` and `shutOf` is untouched — measured, not
asserted: php and julia are byte-identical in every rack column across an arm
that carries the widening and none of the new rows.

The dispatcher's rule became `Troupe.lone`: the C counts live symbols across all
twelve and refuses on the second, which is the whole-roster claim outright rather
than a `rival` pair generalised twelve times.

`_trivia_raw_fi` is in the roster, which is the opposite call from php's two
heredoc members and made on opposite evidence. The heredoc pair's first statement
reads a tag stack, so it is a fence wearing a family's name; `\fi` reads the same
keyword loop as the other eleven and differs only in `is_command_name`, a
parameter of that loop. That difference is where the bytes are: one
`\iffalse … \fi` block is **990 bytes, 18.9% of the file**, against 51 and 76 for
the two verbatim bodies. Shipping the string close alone would have seated eleven
members, collected 127 bytes, and reported a family done.

Result: **108 → 5,246 square, 1,049 → 0 racked, 2,856 → 0 unframed, 72 → 1 root,
damage 1,185 → 0**, and 5,246 of 5,246 bytes matching tree-sitter's derivation at
0.0% crooked (frozen `seat3` oracle, 2026-08-06). Corpus square 272,358 →
300,723 with elixir; no third grammar moved a byte.

The instrument that nearly hid this: a folio is the **pressed table**, and a
`Troupe` seat does not change one — so latex's folio is byte-identical between
the arm scoring 108 and the arm scoring 5,246. Diffing folios across two work
directories proves no table moved and says nothing about whether a derivation
did. The check that answered "no collateral damage" was a scratch tree built from
today's source with only the new rows deleted.
