28 of 33 boards on this disk never read a `square` byte. `onlydamage.py` asks the
same question of the write-ups rather than the boards: **347 pages under
`research/` and `changelog.d/`, of which 116 quote a number of ours -
`damage`, `built`, `standing`, `rubble`, `spoil`, `orphan`, `worth` - and never
one of the oracle's.** It ranks them by how much a sighted reading could move
the conclusion: comparisons made, mentions of a comment/docstring/declared
extra, and whether the page already carries an oracle-free tree-identity proof.

Four of the top ten close for free against boards already taken.
`semi/RESULT-2-seated.md` priced the separator seating at 12,712 bytes of
`unbound`; sighted, its three rows are worth 30,830, 23,878 and 13,874 `square`
- the conclusion holds and the page under-priced its own win by an order of
magnitude on one row. `vacuity/RESULT-2-arms.md` holds. `vacuity/RESULT-5-pairs.md`
does not. The ocaml regression fragment does not.

The three corrections worth having in front of the next lane, all free from the
base board:

- **`damage = 0` means two different things and `damage` cannot tell you which.**
  php stands at 100% with 67,845 square and 0 crooked - real. **elixir stands at
  100% and derives 22,089 of its 46,089 built bytes, 48%, under different parents
  than tree-sitter.** Any page reading elixir as a finished grammar is reading a
  column that was never asked.
- **verilog's 63,937-byte wall is not overturned, it is re-pointed.** The 32,193
  bytes verilog does build carry **2,184 square against 13,128 crooked and
  12,146 unframed** - 7.9% of everything the oracle rules on. Three lanes have
  optimised that row against `damage`; on today's evidence, converting its
  damage into `built` converts unbuilt bytes into misderived ones.
- **haskell's whole board square is 5 bytes**, so every `damage` figure ever
  quoted about a haskell row is a figure about a grammar with no agreement to
  move.

The shape to look for: a page that quotes only our columns, is about a declared
extra, and concludes a seating **cost** something. That is ocaml's fragment
exactly, and it was wrong by 1,169 bytes in the direction the mechanism
predicts. `consort/RESULT-9-reach.md`.
