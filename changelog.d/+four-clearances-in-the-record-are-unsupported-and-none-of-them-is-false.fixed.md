`still.py::vacuous` names an instrument that could not have responded to its own
treatment. Run backwards over the day's collateral claims - every landing in lex,
quire or runtime, none of which can move a pressed table - it separates **four
unsupported clearances from zero false ones**, and that gap is the result.

Cleared, because the lane reached for an artifact the change can move: php's
`encapsed` family (`rack`'s own without-php split reading 204,921 square
identically on both arms - the split is recomputed per arm, so it *can* move);
latex's `marrow` family and elixir's `caesura`, whose dossier names the folio trap
in its own words; kotlin's strings, "compared as trees, not as folio digests";
julia's `abut` markers, 29 of 30 tree-identical with julia's wall moving `lexer` →
`press` as a second falsifier; swift's `multiline_comment`, an honest `--inert`
claim that this moves nothing, which an isolation arm now confirms at zero bytes;
the scanner's `spot` fix, which changed the wall report of 18 of 30 grammars and
was plainly awake; and the resume/splice runtime fix, whose byte-identical `probe`
comes with a reproducer that fires under trace, which is what `--inert` is for.

Unsupported: rows 1, 3 and 5 of the recorded six, per the gate's own backward
pass, plus row 6 - the case it was built for, carrying `folio!` and nothing else
while one grammar went 108 → 5,246 square. And a fourth the backward pass could
not see, because it was never recorded as a pair: `verilog/RESULT-3-provenance.md`
clears a `src/kernel/quire/` edit partly on 27 of 30 folios being byte-identical.
A quire edit cannot move a pressed table, so that half of the argument says
nothing. The other half - 29 of 30 *trees* identical - is responsive and carries
the conclusion, so it is a weak sentence in a sound dossier and not a wrong
result.

The structural gap that matters more than the count: **the gate cannot audit the
pairs that motivated it.** `still against` needs a witness recorded at measurement
time, and none of the historical pins has one. Seventy-six pins survive with their
binaries and their per-file manifests and are still comparable by hand; the gate's
own verdict is only available to pairs taken after it existed. Re-establishing the
four therefore meant rebuilding the arms rather than re-judging the pairs, which
is stronger anyway - it answers *does this break another grammar in today's tree*
instead of *was the original argument sound*.

Four claims were theatre. None of them was covering anything.
