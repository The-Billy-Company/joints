A board arm reading zero has not said the row does nothing - it has said the
grammar's one corpus fixture does not spell the construct. Those are the same
number and no arm of the ablation family can tell them apart, at any subset
size. Adding a third row would not help.

`research/joinery/consort/witnessed.py` is the join nobody ran: for each seated
row, the specimen tier against that row's own isolation arm and against the
control, reporting which specimens **flip**. A specimen that passes with the row
in and fails with it out is a falsifier the row is answering, and it does not
care what the corpus contains.

| | rows |
|---|---|
| **witnessed** - a specimen flips | **11** |
| **corpus-only** - the board moved, no specimen flips | 3 |
| **unwitnessed** - neither moves | **0** |

`multiline_comment/.marrow/.swift_block` - the row the pair sweep called inert -
is one of the eleven, and the only one that reads zero on the board. Nothing in
the roster is in the shape that would earn a deletion argument.

The three `corpus-only` rows are exactly the three whose board worth is
negative: scala's `_indent/.offside/.slashes` (−4,970) and
`block_comment/.marrow/.kotlin_block` (−10,326), and ocaml's
`comment/.marrow/.ocaml_comment` (−721). The three seatings that currently make
their grammar worse are the three with no specimen watching them. That is not a
claim they are wrong; it is a claim that nothing here would notice.

Its sibling `gate.py` prices a pair the way `attribute.py pairs` does - same
`worth`, same `joint`, same `residual`, same retained pins - over fixtures the
caller chooses rather than the corpus board, reusing `standing.rows`/`tops`/
`union` rather than becoming a fourth reader of that render. It reproduces
`pairs.json` to the byte on both `Maps.kt` and `Option.scala` before it is
allowed to disagree with it anywhere else.

Neither tool builds anything; both read the retained `.local/aud-iso/` pins.
`research/joinery/consort/RESULT-4-clearance.md` carries what the fourteen-row
clearance does and does not cover, including the measured fact that every arm in
that family reports `square 0` and `graded —` on all thirty rows, so the whole
clearance is on `damage` and never on agreement with tree-sitter.
