Kotlin's two seatings were priced at +20,737 and +19,229 and called
**cooperating**, a word that is a claim about the two rows. They do not touch.

Each row's effect on its own construct is invariant in the other's state, which
is what independence means. On `val a = 1` / `val b = 2` - a separator and no
string - `worth(_string_start/.fence/.kotlin)` is **0 exactly**, in both states
of the caesura. On `val a = "x"`, removing the fence fails at byte 8 with the
same message whether the caesura is seated or not. Their emitted terminal sets
are disjoint; the one shared name, `_by_delegation_hint`, is an evidence marker
the roster never emits.

What is actually coupled is `Maps.kt`. Its head carries two file annotations and
a string literal:

| | | built | of 35,815 |
|---|---|---:|---:|
| both in | walls at 388 | 35,571 | 99.3% |
| caesura out | walls at **245**, the second `@file:` | 16,342 | 45.6% |
| fence out | walls at **270**, the `"` inside it | 14,834 | 41.4% |
| both out | walls at 245 | 15,893 | 44.4% |

Two gates 25 bytes apart with the whole file behind them. Clearing only the
caesura advances the walk 25 bytes; clearing only the fence advances it none. So
each single-row arm measures *the file behind both gates* and both measure the
**same** 19,678 bytes - which is the 40 KB claim over a 20 KB defect, and it is
one gate read from both sides.

The residual follows the fixture and not the rows, which is checkable and
checked. Two otherwise identical 200-statement kotlin files differing only in
where the single string sits give residual −12 and −202; a file with no string
at all gives **+0**, perfectly additive. A coupling between two rows cannot be
switched off by moving a literal down the page.

So `worth` is a marginal, not a credit, and the column may not be added down
wherever one grammar owns two rows. The only per-row split that sums back to the
pair's +19,678 is Shapley: **+10,593** fence, **+9,085** caesura. Correction
notes are on `vacuity/RESULT-2-arms.md`, `vacuity/RESULT-5-pairs.md` and the
fourteen-rows fragment - no number is edited, because each is true of what it
measured. The historical +20,728 in `orphan/`, `sigil/`, `board/` and
`specimen/` is the fence's landing delta on a tree where the caesura was already
in, so it is that same marginal and it stands.

`research/joinery/consort/RESULT-1-kotlin.md`, with `gate.py`, which re-derives
`pairs.json` to the byte before it is allowed to disagree with it.
