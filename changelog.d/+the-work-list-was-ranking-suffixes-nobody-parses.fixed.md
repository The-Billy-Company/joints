`walls.py board` ranks families by what they cost **on the document** rather
than by an undivided byte total that was 93% fragment.

Every wall already carried `torn` - it stands behind a suffix whose openers the
peel left behind, which is nobody's document - and `run` already told you to
subtract it before quoting a cost. The board summed straight through it, so the
work list it printed was mostly an ordering of text no parser reads.

Splitting the column moves every family. `bracket refused` led at 20,021 bytes
and is 66 on the document; `unrunnable external` sat fourth at 5,629 and is the
real head at 3,284. The remaining tail is 4,591 bytes, not 65,392.

Both columns print, because a fragment wall is a provenance rather than a
dismissal - but only `on doc` is an amount of work. `gate` is unchanged.
