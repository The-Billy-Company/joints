`rack.py`'s headline sentence is "built in a shape tree-sitter does not build",
and **12,439 of the 83,169 crooked bytes were not that**. They are a parent
**both sides agree on** — same name, same namedness, same start — that stops
somewhere else. A moved right edge is a real defect and somebody else's to
price; it is not a disputed derivation, and it was feeding a sentence that says
it is.

`rack.py soft` now subtracts it as a third column, behind whitespace and extras
so the three partition rather than overlap. Without that precedence a blank line
inside a moved `pair` is subtracted twice and toml's HARD goes negative.

| | crooked | blank | extra | edge | soft | **HARD** |
| --- | --- | --- | --- | --- | --- | --- |
| before | 83,169 | 1,386 | 21,645 | — | 27.7% | **60,138** |
| after | 83,169 | 1,386 | 21,645 | 12,439 | 42.6% | **47,699** |

**toml is the whole class and the clean confirmation.** A sibling lane
adjudicated it properly — zero declared conflicts, zero contested cells across
175 states, both parsers hanging the `comment` under the same `pair`, joints's
`pair` merely ending at byte 7 with a child starting at 9 — and the verdict was
against the instrument, not the parser. `soft` now reads toml at **29 crooked,
100.0% soft, 0 HARD**. It is 96% of latex and 80% of zig as well.

That lane priced the class at **8,334** bytes and put the defended figure at
52,439; this rule reads **12,439** and 47,699. The 4,105 is not reconciled here
and is most likely the precedence — I charge whitespace and extras first and
only then ask whether the edge moved. Both numbers agree on the direction and
the order of magnitude, and both say the published 60,138 was too high. Note
that this delta and the `unframed` charge landing in the same release point
opposite ways; summed they look like nothing happened, and they are not the same
measurement.

**The free finding.** `standing.py` has been printing

```text
UNSOUND: 1 loose, 0 disorder, 0 torn
  [child outside its parent: comment [47, 56) in pair [27, 45)]
```

on the toml row the whole time, on a grammar the board scores **100.0%
standing** and `whole`, and no board read it. That is joints's own words about
its own forest, and it is the same defect arrived at from the other side — the
parser said so before any oracle was consulted. `rack board` now reprints every
row carrying a soundness complaint underneath the table, with the complaint's
own text. One row carries one today; the mechanism is there for the next.
