Elixir's 22,089 crooked bytes were one cell. State 272 has the inner call
`f(x)` complete beside the same rule's dot in front of its own optional
`do_block`, and on `do` it reads on — taking the block as another argument to
`f(x)` instead of folding and handing the `do` up to the `defp` that is holding
a dot in front of exactly it.

Nothing refused. Elixir reads `accepted, 1 root` over all 46,089 bytes and hands
back a well-formed wrong tree, so `inquest` has no stop to attribute and `plumb`
scores the row a perfect **0 askew**. Only a derivation comparison sees it —
which is why elixir is the one row of seventeen that reaches whole and then
fails both interior questions, and the only thing standing between "17 parse
whole" and "16 whole on all three axes".

**The repair is two halves and neither one works alone.** In
`src/press/bench.zig`, the `continues` rung keeps an optional tail wherever the
short production stands complete beside the long one's dot — right where the
fold would *strand* the token (`defmodule Foo do` has nothing above waiting for
it) and wrong where the token is what an enclosing rule is waiting for. A strand
test pops the handle, takes the goto and asks whether the state it lands in has
an edge on `t`, chaining while the answer is another fold. That fixes the table
and not the tree: on its own it reads elixir **22,089 → 22,721** and turns the
`do-block-without-inner-call.ex` control red. With both actions in the cell the
parse forks and `collapse` merges the limbs back at **equal `heft` and equal
`rank`**, so `Reading.beats` falls through to a strict `rank <` and keeps
whichever limb was born first, which is the shift. The press was handing the
loop a correct choice and the loop was discarding it on a tie-break that is not
a judgement — it is position in `next`, the driver's own enqueue order. Naming
that case in `collapse` and giving it to the challenger is the second half. It
is not the third rung `Reading.beats` declines: that one fires wherever `heft`
alone ties, and this fires only where `heft` and `rank` are both equal, which
`beats` resolves with a `<` and so never separates at all. Both faces of the
coin are measured, because the control arm is the other face.

Paired: **elixir 22,089 → 0**, corpus **52,635 → 30,573 (−41.9%)**, elixir
specimens **4/5 → 5/5**, `rack.py verify` 38 of 38 and `plumb.py verify` 10 of
10. `built` (46,089) and `roots` (1) unmoved, so the bytes went into `square`
(23,879 → 46,089) rather than out of the judged population, and elixir finishes
with 46,089 square and nothing in any other bucket. Twenty-eight of the thirty
rows are byte-identical across all four arms. The whole cost is swift **+27**,
and no square byte pays it: swift's `square` is 14,419 in every arm and the 27
come out of `unframed`.

This row read **22,210** before `plumb.hurt()` was corrected from an ancestry
test to the byte's innermost cover, and the corpus read 60,964. Same repair,
different ruler; a crooked figure quoted across that correction is not
comparable, and these are the corrected ones.

**Not the same fix as swift's trailing closure, though it is the same shape.**
A block-introducing token arriving where an inner construct is complete and an
outer one is waiting, taken by the inner one — `do` for `f(x)` is `{` for the
`if let`, one for one. But swift's site is a ranked cell where an unranked fold
was erasing an authored reading, and this one carries no conflict record at all:
`[prec 0 left]` throughout, resolved silently by the tail rung. There is no
authored reading here to order, and the swift repair is already in the arm this
was measured against. It does not reach ocaml, sql or julia either — all three
are byte-identical across every arm, and their crooked bytes are a different
construct each.

Measured on four pinned arms, one variable each, 30 of 30 verdicts live per arm,
built from the working tree with `src/kernel/lex/{scanner,outside,writ}.zig`
rolled back to `7d72a5d` — the in-flight bracket-order seat in those three files
was refusing a real `do` and taking elixir to 119 roots when this was written.
`7d72a5d` alone does not build: its `gather.zig` reads `split.sided`, which
exists only in the uncommitted `forks.zig`. A fifth pin off the live tree with
nothing rolled back reads elixir the same — 46,089 square, 0 crooked — and
differs from the paired arm on exactly one row of thirty, haskell, which is that
lane's own row under its own change. `research/joinery/elixir/RESULT-2-do-block.md`.
